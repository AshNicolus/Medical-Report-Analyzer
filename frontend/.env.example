# Frontend Environment Variables
# Copy this file to .env.local for local development

# Backend API Base URL (optional - proxy is configured in vite.config.js for local dev)
# Leave empty to use the Vite dev server proxy (/api -> http://localhost:8080)
# Set this for production builds or custom backend URLs
VITE_API_BASE_URL=
